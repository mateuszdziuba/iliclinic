---
phase: 05-sample-content-launch-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified: []
autonomous: false
requirements:
  - BUILD-01
  - MOBILE-01

must_haves:
  truths:
    - "pnpm build completes with exit code 0"
    - "No /undefined URLs appear in build output or generated HTML"
    - "All mobile layouts render correctly (no horizontal overflow, nav works)"
  artifacts:
    - path: "dist/"
      provides: "Built static site"
      contains: "index.html"
  key_links:
    - from: "src/pages/[location]/technologie/[slug].astro"
      to: "dist/pruszkow/technologie/"
      via: "getStaticPaths using getCollection('technologie')"
      pattern: "laser-frakcyjny|ultradzwieki-hifu"
---

<objective>
Run the production build, verify it succeeds without errors and without any `/undefined` URLs, then confirm mobile layouts on all key pages.

Purpose: Phase 5 gates on a clean build and a presentable mobile experience before launch.
Output: Confirmed green build, zero `/undefined` URLs, human-verified mobile layouts.
</objective>

<execution_context>
@/Users/mati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-sample-content-launch-readiness/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production build and verify no undefined URLs</name>
  <files></files>
  <read_first>
    - .planning/phases/05-sample-content-launch-readiness/05-01-SUMMARY.md — confirm content files were created
  </read_first>
  <action>
Run the build from the project root (`/Users/mati/Mine/iliclinic`):

```bash
cd /Users/mati/Mine/iliclinic && pnpm build 2>&1
```

If the build exits non-zero, read the full error output and fix the root cause before proceeding. Common causes:
- Missing required frontmatter fields in a .md file — fix the offending file
- TypeScript type errors in .astro files — fix in the relevant page
- Missing import — add the import

After a successful build (exit 0), scan for `/undefined` in generated HTML:

```bash
grep -r "/undefined" /Users/mati/Mine/iliclinic/dist/ --include="*.html" -l 2>/dev/null
```

If any files are returned, read one to identify which template generates the bad URL, then fix the Astro page (use `.id` not `.slug` per STATE.md).

Re-run build after any fix.
  </action>
  <verify>
    <automated>cd /Users/mati/Mine/iliclinic && pnpm build 2>&1 | tail -5 && echo "---" && grep -r "/undefined" /Users/mati/Mine/iliclinic/dist/ --include="*.html" -l 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits with code 0
    - `grep -r "/undefined" dist/ --include="*.html" -l` returns 0 files
    - dist/pruszkow/technologie/laser-frakcyjny/index.html exists
    - dist/ostroleka/technologie/laser-frakcyjny/index.html exists
  </acceptance_criteria>
  <done>Build succeeds, zero /undefined URLs found in dist/, technology pages generated for both locations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human mobile layout verification</name>
  <what-built>
    Production build complete. All pages built — location landing pages, zabiegi index/slug, technologie index/slug, o-nas, kontakt.
  </what-built>
  <how-to-verify>
    Start the preview server:

    ```bash
    cd /Users/mati/Mine/iliclinic && pnpm preview
    ```

    Open browser DevTools → Toggle device toolbar → select iPhone 14 (390px wide) or equivalent.

    Check these pages for each location (pruszkow and ostroleka):

    1. `/pruszkow` — hero text not overflowing, CTA buttons full-width, no horizontal scroll
    2. `/pruszkow/zabiegi` — treatment cards stack vertically, category filter scrollable, search input full-width
    3. `/pruszkow/zabiegi/botoks` — bento grid stacks to single column, price/duration chips wrap correctly
    4. `/pruszkow/technologie` — technology cards stack vertically
    5. `/pruszkow/technologie/laser-frakcyjny` — hero stacks (image below title), bento sidebar stacks below content
    6. `/pruszkow/o-nas` — sections readable, no text clipped
    7. `/pruszkow/kontakt` — map embed full-width, contact rows readable
    8. Header — hamburger menu opens and closes, all nav links accessible on mobile

    Accept if: all pages render without horizontal overflow, text is legible, navigation works.
    Reject and describe specific page + issue if anything is broken.
  </how-to-verify>
  <resume-signal>Type "approved" if all pages pass, or describe the specific issue(s) found (e.g., "technologie hero overflows on mobile at /pruszkow/technologie/laser-frakcyjny")</resume-signal>
</task>

</tasks>

<verification>
Build verification (automated):

```bash
# Build exits 0
cd /Users/mati/Mine/iliclinic && pnpm build; echo "Exit: $?"

# No /undefined in output HTML
grep -r "/undefined" /Users/mati/Mine/iliclinic/dist/ --include="*.html" -l 2>/dev/null | wc -l
# Expected: 0

# Technology pages generated
ls /Users/mati/Mine/iliclinic/dist/pruszkow/technologie/
ls /Users/mati/Mine/iliclinic/dist/ostroleka/technologie/
# Expected: laser-frakcyjny/ and ultradzwieki-hifu/ directories in each
```

Mobile verification: human checkpoint (Task 2).
</verification>

<success_criteria>
- pnpm build exits 0 with no errors
- grep for /undefined in dist/ returns 0 files
- dist/pruszkow/technologie/laser-frakcyjny/index.html exists
- dist/ostroleka/technologie/laser-frakcyjny/index.html exists
- Human confirms mobile layouts are acceptable on all 8 page types checked
</success_criteria>

<output>
After completion, create `.planning/phases/05-sample-content-launch-readiness/05-02-SUMMARY.md`
</output>
