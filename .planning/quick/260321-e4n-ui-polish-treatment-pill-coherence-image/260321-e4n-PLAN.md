---
phase: quick
plan: 260321-e4n
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/[location]/zabiegi/index.astro
  - src/pages/[location]/zabiegi/[slug].astro
  - src/pages/[location]/o-nas.astro
autonomous: true
requirements: [UI-POLISH]

must_haves:
  truths:
    - "Filter pill and nav pill share identical frosted-glass styling"
    - "Treatment detail hero image has significantly rounder corners and warm brand overlay"
    - "O-nas equipment section has no circular dot decorations"
    - "Mobile spacing is comfortable across treatments list and detail pages"
  artifacts:
    - path: "src/pages/[location]/zabiegi/index.astro"
      provides: "Filter bar pill with nav-matched styling"
    - path: "src/pages/[location]/zabiegi/[slug].astro"
      provides: "Hero image with rounder corners and warm overlay"
    - path: "src/pages/[location]/o-nas.astro"
      provides: "Equipment section without circle decorations"
  key_links: []
---

<objective>
Visual polish pass: unify floating pill styles, enhance treatment hero images, clean up o-nas decorations, and fix mobile spacing.

Purpose: Achieve visual coherence across all key pages before launch.
Output: Updated page files with consistent styling.
</objective>

<execution_context>
@/Users/mati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/Header.astro
@src/pages/[location]/zabiegi/index.astro
@src/pages/[location]/zabiegi/[slug].astro
@src/pages/[location]/o-nas.astro

<interfaces>
<!-- Nav pill reference style (Header.astro line 29) — the gold standard: -->
bg-white/75 backdrop-blur-md border border-white/40 shadow-[0_8px_24px_rgba(0,0,0,0.06)] rounded-full

<!-- Filter pill current style (zabiegi/index.astro line 68) — close but slightly different: -->
bg-white/85 backdrop-blur-xl border border-white/40 shadow-[0_8px_24px_rgba(0,0,0,0.06)] rounded-full

<!-- Treatment hero image current (zabiegi/[slug].astro line 111): -->
rounded-t-[40px] lg:rounded-t-[60px]
<!-- Has gradient overlay: from-primary/20 via-transparent to-transparent -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify filter pill with nav + fix mobile spacing on treatments pages</name>
  <files>src/pages/[location]/zabiegi/index.astro, src/pages/[location]/zabiegi/[slug].astro</files>
  <action>
**Filter bar pill (zabiegi/index.astro):**
- Line 68: Change `bg-white/85 backdrop-blur-xl` to `bg-white/75 backdrop-blur-md` to exactly match the nav pill in Header.astro. The border and shadow are already identical.
- On the filter bar container (line 66), ensure the sticky `top` value accounts for the nav height. Currently `top-[88px]` — verify this still works. Add `px-6` instead of `px-4` for consistent horizontal padding with the rest of the page.
- On mobile, ensure the filter pill wraps gracefully: the category buttons inside the pill should scroll horizontally on small screens rather than wrapping to multiple lines. Add `overflow-x-auto flex-nowrap` to the `#filter-buttons` div (line 83), and add `whitespace-nowrap` to prevent button text wrapping. Add `scrollbar-hide` utility or `-webkit-scrollbar: none` via inline style.

**Treatment detail hero image (zabiegi/[slug].astro):**
- Line 111: Increase corner radius significantly from `rounded-t-[40px] lg:rounded-t-[60px]` to `rounded-[32px] lg:rounded-[48px]` (all corners rounded, not just top, making it a contained card shape).
- Line 112: Enhance the gradient overlay from `from-primary/20` to a warmer tone: `from-[#8B6F47]/30 via-[#8B6F47]/10 to-transparent` — this uses a warm brown that matches the brand's earth-tone palette. Keep `pointer-events-none` and `z-10`.
- Add a subtle warm color-wash overlay on the image container: add a second overlay div after the gradient: `<div class="absolute inset-0 bg-[#C4A882]/8 mix-blend-multiply z-10 pointer-events-none"></div>` for a warm-toned film effect.
- Mobile image height: change `h-[50vh]` to `h-[40vh]` on mobile to prevent the image from pushing content too far down on small screens.

**Mobile spacing fixes on [slug].astro:**
- Line 69: The hero text block has `pb-12 lg:pb-16` — reduce to `pb-8 lg:pb-16` to tighten mobile.
- Line 75: The h1 uses `text-5xl sm:text-6xl lg:text-7xl xl:text-8xl` — reduce mobile to `text-4xl sm:text-5xl lg:text-7xl xl:text-8xl` for better fit.
- Line 137: Bento grid `pt-8` — change to `pt-6 lg:pt-8`.
- Line 83: Quick facts row — add `gap-2 sm:gap-3` instead of just `gap-3`.

**Mobile spacing fixes on index.astro:**
- Line 50: Hero section — change `pt-32 pb-16` to `pt-28 pb-12 lg:pt-40 lg:pb-20` for tighter mobile.
- Line 55: h1 — reduce mobile from `text-5xl` to `text-4xl`: `text-4xl sm:text-6xl lg:text-8xl`.
  </action>
  <verify>Run `npx astro build 2>&1 | tail -5` — build succeeds with no errors. Visually inspect that changes look correct in both files.</verify>
  <done>Filter pill matches nav pill styling exactly (bg-white/75 backdrop-blur-md). Treatment hero image has rounded-[32px] corners on all sides, warm brown overlay, and tighter mobile sizing. Mobile spacing is reduced on both pages.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up o-nas equipment section — remove circle decorations</name>
  <files>src/pages/[location]/o-nas.astro</files>
  <action>
**Equipment list items (lines 163-210):**
Each equipment item has a circular dot decoration on the right side:
```
<div class="w-8 h-8 rounded-full border border-primary/20 flex items-center justify-center group-hover:bg-primary group-hover:border-primary transition-all shrink-0">
  <div class="w-1.5 h-1.5 bg-primary group-hover:bg-surface rounded-full transition-colors"></div>
</div>
```

Remove these circular button decorations from ALL four equipment items (AlexDual Xlase, Dermapen 4.0, Gold Needle RF, AquaSure H2). Delete the entire `<div class="w-8 h-8 rounded-full...">` wrapper and its inner dot for each item.

Keep the rest of the list item structure intact — the flex layout with the equipment name and subtitle on the left, the hover state on the row, and the border-bottom separators.

After removing the circle, also remove `justify-between` from each item's parent flex container since there is no longer a right-side element, replacing it with just `flex items-center`. Or keep `justify-between` — it will still work fine with a single child; either approach is acceptable.

**Do NOT touch the Values section (lines 218-265)** — those circular icons (w-16 h-16 with SVGs inside) are different; they are meaningful icon containers for the three values (Bezpieczenstwo, Naturalnosc, Indywidualnosc) and should stay.
  </action>
  <verify>Run `npx astro build 2>&1 | tail -5` — build succeeds. Grep for `w-8 h-8 rounded-full` in o-nas.astro — should return zero matches (confirming circles removed). Grep for `w-16 h-16` in o-nas.astro — should still return 3 matches (values section icons preserved).</verify>
  <done>All four equipment list items no longer have the circular dot decoration. Values section icons are untouched. Page builds without errors.</done>
</task>

</tasks>

<verification>
- `npx astro build` completes without errors
- Filter pill on zabiegi page uses `bg-white/75 backdrop-blur-md` matching nav
- Treatment detail image has `rounded-[32px]` and warm overlay
- O-nas equipment items have no circle decorations
- Values section circular icons remain intact
</verification>

<success_criteria>
- All three page files updated and building successfully
- Filter pill visually matches nav bar pill (frosted glass, same opacity/blur)
- Treatment hero image has rounder corners on all sides with warm brown overlay
- Mobile spacing tightened on both treatment pages
- O-nas equipment circles removed, values icons preserved
</success_criteria>

<output>
After completion, create `.planning/quick/260321-e4n-ui-polish-treatment-pill-coherence-image/260321-e4n-SUMMARY.md`
</output>
